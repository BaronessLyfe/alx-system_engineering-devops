#include <stdio.h>
#include <unistd.h>

/**
 * main - PID
 *
 * Return: Always 0.
 */
int main(void)
{
    pid_t my_pid;

    my_pid = getpid();
    parent_id=getppid();
    printf("%d\n", my_pid),(%d\n, parent_id);
    return (0);
}
